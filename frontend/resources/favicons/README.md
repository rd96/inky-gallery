# Favicon variants

Alternate crops of `src/assets/login-background.jpeg` used as the fill for the
"IG" pixel-letterform favicon, all sharing the same letterform geometry and a
transparent background. `favicon-orange.svg` is the one currently in use at
`public/favicon.svg`.

To switch, copy one of these over `public/favicon.svg`.
