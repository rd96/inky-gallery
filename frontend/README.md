# Frontend

React + TypeScript + Vite SPA for Inky Gallery.

## Setup

Recommended to use Visual Studio Code.

```bash
npm install
npm run dev      # dev server with HMR
npm run build    # type-check + production build
npm run lint     # Oxlint
```
